# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Email Configuration (Using Python smtplib)
# Leave EMAIL_HOST_USER and EMAIL_HOST_PASSWORD empty for development mode (emails will print to console)
# Set these for production email sending

DEFAULT_FROM_EMAIL=noreply@lwazi-blue.co.za

# SMTP Server Settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# SMTP Authentication (required for production)
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# For Gmail, use an App Password:
# 1. Enable 2-factor authentication on your Google account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate an app password for "Mail"
# 4. Use that password as EMAIL_HOST_PASSWORD

# For other providers:
# - Outlook/Hotmail: smtp-mail.outlook.com:587
# - Yahoo: smtp.mail.yahoo.com:465 (use SSL instead of TLS)
# - Custom SMTP: your-smtp-server.com:587

# =====================================================
# DATABASE CONFIGURATION
# =====================================================
# Development (DEBUG=True): Automatically uses SQLite3
# Production (DEBUG=False): Uses DATABASE_URL

# DATABASE_URL format examples:
# PostgreSQL: postgresql://username:password@localhost:5432/database_name
# PostgreSQL (alternative): postgres://username:password@localhost:5432/database_name
# MySQL: mysql://username:password@localhost:3306/database_name
# SQLite: sqlite:///path/to/database.db

# For production (uncomment and configure):
# DATABASE_URL=postgresql://lwazi_user:secure_password@localhost:5432/lwazi_blue

# For PostgreSQL setup:
# 1. Install PostgreSQL on your server
# 2. Create database: CREATE DATABASE lwazi_blue;
# 3. Create user: CREATE USER lwazi_user WITH PASSWORD 'secure_password';
# 4. Grant permissions: GRANT ALL PRIVILEGES ON DATABASE lwazi_blue TO lwazi_user;
# 5. Install Python package: pip install psycopg2-binary

# For MySQL setup:
# 1. Install MySQL on your server
# 2. Create database: CREATE DATABASE lwazi_blue CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
# 3. Create user: CREATE USER 'lwazi_user'@'localhost' IDENTIFIED BY 'secure_password';
# 4. Grant permissions: GRANT ALL PRIVILEGES ON lwazi_blue.* TO 'lwazi_user'@'localhost';
# 5. Install Python package: pip install mysqlclient


